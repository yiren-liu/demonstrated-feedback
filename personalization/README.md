# Contextualized Steering Vectors

Run from the project root directory:

```bash
cd /home/cz468/personalization
python -m single_vector.evaluate --with_steering ... --without_steering ...
python -m multi_vectors.cluster --dataset ...
python -m single_vector.extract --dataset ...
```